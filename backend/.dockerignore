# Ignore node modules (they will be reinstalled in image)
node_modules

# Ignore local uploads - mounted via volume in compose
uploads

# Environment and config files
.env
.env.local

# Docker-specific files (not needed inside container)
Dockerfile
docker-compose.yml

# Development + system files
*.log
npm-debug.log*
.vscode
.idea
.DS_Store

# Git + docs
.git
.gitignore
README.md
